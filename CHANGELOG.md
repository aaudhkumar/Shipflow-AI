# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.0.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]
### Added
- Initial monorepo setup with `pnpm`.
- Next.js frontend (`apps/web`).
- Express API with tRPC (`apps/api`).
- Drizzle ORM integration (`packages/db`).
- Better-Auth configuration (`packages/auth`).
- Inngest background worker support (`packages/workflow`).
- Razorpay billing integration.
- GitHub App webhook ingestion for PR and Repository tracking.
- Interactive API Reference via Scalar (`/docs`).
